version: '3.8'
services:
  pos-management:
    build: .
    image: pos-management:latest
    ports:
      - "8080:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    volumes:
      - ./:/src:ro
    restart: unless-stopped
